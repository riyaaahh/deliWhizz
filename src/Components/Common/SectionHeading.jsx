import React from 'react'

const SectionHeading = ({ text }) => {
    return (
        <div className="font-bold text-md text-deli-black-500">
            {text}
        </div>
    )
}

export default SectionHeading